# 용어사전 (쉬운 말 버전)

> 코드와 문서에 나오는 어려운 말들을 **쉬운 한국어**로 풀었습니다.
> 각 항목 끝의 `코드에서: ...` 는 그 말이 코드에서 어떤 이름(변수/함수)으로 나오는지입니다.
> 코드 읽다 모르는 단어 나오면 여기서 찾으세요.

---

## 1. 로봇이 움직이는 기본

- **노드 (node)** = 바닥 **칸**. 로봇은 칸에서 칸으로 이동. 번호 1~48.
  `코드에서: node, current_node`
- **마커 (marker)** = 바닥에 붙은 표식(QR 비슷). 로봇이 칸에 도착하면 이걸 읽어 "나 여기 도착" 보고.
  `코드에서: marker, _handle_marker_report`
- **forward / turn / lift** = 로봇한테 보내는 명령. 앞으로 한 칸 / 방향 틀기 / 선반 들기·내리기.
- **cmd_ack** = "명령 다 했어요" **완료 신호**. (방향 틀기·리프트 끝나면 로봇이 보냄)
  `코드에서: cmd_ack, _handle_cmd_ack`
- **heading** = 로봇이 **보고 있는 방향**. 북/동/남/서 = 0/90/180/270.

---

## 2. 서로 안 부딪치게 하는 것

- **예약 (reservation)** = "이 칸은 내가 쓸 거"라고 **미리 찜**해두는 것.
  `코드에서: reservation, commit`
- **락 (lock) / 통행권** = 예약의 작은 버전. **"지금 이 칸 들어간다"고 한 칸만** 잠그는 것. = "한 칸 보고 들어가기".
  `코드에서: try_lock, unlock`
- **교착 (deadlock)** = 두 로봇이 **서로 길을 막아 둘 다 못 움직이는** 상태. (= 우리가 고치려는 "멈춤")
  `코드에서: deadlock_detector, _resolve_deadlock`
- **드리프트 (drift)** = **미리 짠 계획과 실제가 시간 갈수록 어긋나는** 것. (회전이 느려서 생김)
- **lockstep** = "모두 똑같이 한 칸씩 동시에 간다"는 **(틀린) 가정.** 드리프트의 원인.
- **swap / 정면 충돌** = 두 로봇이 **자리를 맞바꾸려다** 부딪침.

---

## 3. 작업대 앞에서 줄 서는 것

> 작업대(W) 가는 길은 좁아서 한 번에 **한 대만** 들어갈 수 있음. 그래서 줄서기 규칙이 있음.

- **회랑 (corridor)** = 작업대로 들어가는 **좁은 통로.** 한 대만 점유 가능.
  `코드에서: corridor, staging_manager`
- **게이트웨이 (gateway)** = 회랑 **입구 칸.**
  `코드에서: gateway_node`
- **스테이징 (staging)** = 회랑이 차 있을 때 **대기하는 자리.** "줄 서서 기다리는 곳."
  `코드에서: staging_node, staging_manager`
- **트리거 (trigger)** = 나가는 로봇이 지나가면 **"이제 비었어"** 알리는 표식 칸. → 기다리던 로봇 들여보냄.
  `코드에서: trigger, handle_marker_trigger`
- **포워딩 (forwarding)** = 한 선반을 작업대 A에서 끝낸 뒤, 제자리에 안 돌려놓고 **그대로 작업대 B로 또 가져가는** 것.
  `코드에서: FORWARD_SHELF, is_forwarding`
- **인터셉트 (intercept)** = 선반을 제자리로 돌려놓으러 가는 중에 그게 **또 필요해지면, 돌아가지 말고 중간에 가로채서** 작업대로 보내는 것.
  `코드에서: intercept, Node U`

---

## 4. 코드 구조에서 자주 보이는 것

- **in_flight** = "지금 보낸 명령, 아직 완료 신호 안 옴" 상태. (한 번에 명령 **하나만** 보냄)
  `코드에서: in_flight, _in_flight_cmds`
- **B-selfguard** = **"로봇이 실제로 도착했을 때만 다음 계획을 짠다"** 는 안전 규칙. (도착도 안 했는데 미리 계획하면 한 칸씩 밀리는 버그 방지)
- **yield (양보)** = 교착일 때 한 로봇이 **옆으로 비켜주는** 것.
- **preempt (선점)** = **먼저 자리를 차지**하는 것.
- **F-노드 / Node U** = 설계도(`FLOWCHART.md`)의 **분기점 이름.** F = "선반이 지금 어디 있나?" 확인하는 갈림길 / U = 위의 "인터셉트" 갈림길.
- **subtask (서브태스크)** = 한 주문을 잘게 쪼갠 **작은 일 단위.** (선반으로 감 → 들기 → 작업대로 → 대기 → 돌려놓기)

---

## 5. 로봇 상태 (6단계, 쉬운 말)

| 코드 이름 | 쉬운 뜻 |
|---|---|
| IDLE | 놀고 있음 (할 일 없음) |
| MOVING_TO_SHELF | 선반 가지러 가는 중 |
| PICKING_UP_SHELF | 선반 드는 중 |
| DELIVERING_TO_WS | 선반 들고 작업대로 가는 중 |
| WAITING_FOR_PICK | 작업대에서 사람이 물건 집기를 기다림 |
| RETURNING_SHELF | 선반 제자리로 돌려놓으러 가는 중 |

---

> 모르는 단어가 더 있으면 여기에 계속 추가하세요. "쉬운 뜻 + 코드에서 보이는 이름" 한 줄이면 됩니다.
