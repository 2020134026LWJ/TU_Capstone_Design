# 검증 체크리스트 — 경로계획 / 충돌회피 / 교착회피 / 인터셉트 / STG

> 작성일: 2026-04-29
> 목표: Isaac Sim 런타임 검증 전, 4개 영역의 알고리즘 정합성을 **정적 분석**으로 확인
> 후속 단계: Stage 2 (pytest 단위 테스트) → Stage 3 (Isaac Sim 런타임)

---

## 진행 현황

- [x] **영역 1: 충돌회피 (cmd-based)** ✅ 완료 (2026-04-29)
- [x] **영역 2: 교착회피** ✅ 완료 (2026-04-29)
- [x] **영역 3: 인터셉트 (Node U)** ✅ 완료 (2026-04-29) — NG 1건 발견 → ✅ **수정 완료 (2026-04-29, FLOWCHART 수정 27)**
- [x] **영역 4: STG (스테이징 게이팅)** ✅ 완료 (2026-04-29)
- [x] **종합 요약 + 우선순위** ✅ 완료 (2026-04-29)

## 범례
- ✅ **OK**: 코드 + 플로우차트 일치, 엣지케이스 처리됨
- ⚠️ **의심**: 동작은 하지만 엣지케이스 / 회귀 우려
- ❌ **NG**: 명백한 버그 또는 누락
- 📝 **메모**: 추가 조사 필요

---

## 영역 1: 충돌회피 (cmd-based)

**관련 파일**: `server/request_handler.py`, `server/path_planner.py`

**핵심 메커니즘**:
- `_reserved_nodes`: forward 명령 발행 시 목적지 예약 → 마커 도착 시 해제
- `_blocked_robots`: 충돌 예상 로봇을 보류 큐로 이동
- `_send_next_command()`: forward 발행 전 안전성 체크
- `_is_safe_to_resume()`: 다음 노드 진입 가능 여부

### 체크 항목

#### 1.1 ✅ `_send_next_command()` (request_handler.py:1320~1361)
- forward 명령에서만 충돌 체크 (turn/lift은 skip) — 회전은 in-place라 OK
- `next_node is None` 체크 (line 1335) — 수정 26 적용 확인됨
- `occupied = other.current_node == next_node` (line 1345) — 수정 22 적용 확인됨 (`_claimed_nodes` 제거)
- `reserved = self._reserved_nodes.get(next_node) == other_rid` (line 1346) — 시간 예약 체크
- 통과 시 `_reserved_nodes[next_node] = rid` (line 1354) 등록 + `_blocked_robots.discard(rid)` (line 1358)

#### 1.2 ✅ `_handle_marker_report()` (request_handler.py:411~505)
- 마커 도착 → `_reserved_nodes[node]` 해제 (line 424~425)
- `_send_next_command(rid)` (line 498) → 다음 명령 발행
- `_retry_blocked_robots()` (line 493, 501) → 블록된 다른 로봇 재시도

#### 1.3 ✅ `_handle_cmd_ack()` turn 분기 (request_handler.py:796~806)
- `turn_left/right/180` 시 `robot.heading` 업데이트
- `_send_next_command(rid)` 호출 (line 804)
- **`_retry_blocked_robots()` 호출 (line 805)** — 수정 26 핵심: turn 완료 시도 → 교착 해제 트리거

#### 1.4 ✅ `_get_blocker_of()` (request_handler.py:542~557)
- forward 명령 큐인 경우만 blocker 탐색 (`command_queue[0] != "forward"` skip)
- 점유(`current_node`) + 예약(`_reserved_nodes`) 둘 다 검사

#### 1.5 ✅ `_retry_blocked_robots()` (request_handler.py:530~540)
- `_blocked_robots`에서 정렬된 순서로 재시도 (`sorted()`로 결정적 처리)
- 재시도 실패 + blocker도 blocked 상태 → `_resolve_deadlock()` 호출 (교착 감지)
- iteration 중 `_blocked_robots` 변경 대비 `.copy()` 사용 + 재확인

#### 1.6 ✅ `_get_next_node_by_heading()` (request_handler.py:1362~1387)
- 4개 heading 모두 처리 (0/90/180/270)
- 인접 노드 없으면 None 반환 (벽/모서리 안전 처리)

#### 1.7 ⚠️ **의심 1**: `next_node is None` 시 영구 stuck 가능성
- `_blocked_robots.add(rid)` 후 return False
- 같은 상태로 `_retry_blocked_robots()` 호출 시 또 None → 무한 retry
- **원인**: `_path_to_commands`가 잘못된 forward 명령을 큐에 넣은 경우 (heading 미스매치)
- **영향**: 시뮬에서 발생하면 즉시 발견 가능, 실물 동작 시 심각
- **권장**: 진단 로그 강화 + 1~2회 retry 후 force-replan 폴백

#### 1.8 ⚠️ **의심 2**: 예약 영구 lock (마커 미감지 케이스)
- forward 발행 → `_reserved_nodes[next_node]` 등록
- AGV가 next_node 도달했으나 ArUco 미감지 → marker_report 안 옴 → 예약 영구 lock
- **현재 방어**: 없음 (타임아웃 메커니즘 부재)
- **영향**: 실물 환경에서 카메라 노이즈/조명 문제 시 데드락
- **권장**: cmd 발행 시 타임스탬프 기록 → N초 후에도 marker 안 오면 강제 해제 + 알람

#### 1.9 ⚠️ **의심 3**: 재계획 시 `_reserved_nodes` 미해제
- `_plan_and_publish_move()` (line 1389~1482) — 재계획 시 `_clear_robot_reservation` 미호출
- `_clear_robot_reservation`은 task 완료 시(line 888, 959, 1523)만 호출됨
- **케이스**:
  - `_replan_for_placed_shelf()` (line 596~613): putdown 후 다른 carrying 로봇 재계획 — 그 로봇이 forward 진행 중일 수 있음
  - `_resolve_deadlock()` Strategy 2: yield 노드로 새 경로 → 기존 예약 잔존 가능
  - `_reroute_robot_after_skip()` (line 1507~1536): 포워딩 스킵 후 재라우팅
- **분석**: 실제로 forward 명령이 발행됐다면 AGV는 그 next_node로 물리적 commit 상태 → 마커 도착 시 자동 해제됨. 따라서 phantom reservation은 아니지만, **재계획 직후 `_send_next_command`가 호출되면 새 경로의 첫 명령과 commit 중인 이동이 충돌할 수 있음**
- **권장**: 재계획 진입 시 robot의 `command_queue` 상태 확인 + AGV 정지 보장 필요 (현재는 명시적 정지 없음)

#### 1.10 ✅ `_find_yield_node()` (request_handler.py:559~594)
- `shelf_manager.all_shelf_nodes` 사용 (수정 26 — `path_planner.shelf_nodes` 폐기)
- `contested_node` + 선반 노드 + 다른 로봇 점유/예약 노드 모두 제외

### 결론

**상태**: 핵심 로직은 잘 구현됨. 시뮬레이터 환경(이상적 마커 감지)에서는 안정적으로 동작 예상.

**주요 리스크 (실물 또는 엣지케이스)**:
1. 의심 2 (마커 미감지 → 예약 lock) — 실물 환경에서 가장 위험
2. 의심 3 (재계획 시 reservation 미해제) — 동시성 시나리오에서 desync 위험
3. 의심 1 (None forward → 영구 stuck) — 코드 버그 시 발현, 빠른 발견 가능

**권장 개선**:
- 예약 타임아웃 메커니즘 추가 (의심 2)
- 재계획 시 명시적 reservation clear + AGV 정지 시퀀스 (의심 3)

---

## 영역 2: 교착회피

**관련 파일**: `server/request_handler.py`, `server/path_planner.py`

**핵심 메커니즘**:
- `_retry_blocked_robots()`: 마커 도착 / turn cmd_ack 시 호출
- `_resolve_deadlock()`: 전략 1 (A* 우회) → 전략 2 (yield 옆 칸 이동)
- `_find_yield_node()`: 선반 노드 제외하고 빈 노드 탐색
- `astar_with_time(turn_penalty=0.3)`: 회전 최소화

### 체크 항목

#### 2.1 ✅ 교착 감지 트리거 (request_handler.py:530~540)
- `_retry_blocked_robots()` 호출 시 검사
- 호출 지점: `_handle_marker_report` (line 493, 501), `_handle_cmd_ack` turn 분기 (line 805)
- 조건: A가 blocked AND blocker(B)도 blocked → `_resolve_deadlock(A, B)`

#### 2.2 ✅ 우선순위 결정 (request_handler.py:628~636)
- 선반 운반 중 (`carrying_shelf`) > 미운반
- 동급일 때 `max(rid_a, rid_b)` → yield, `min` → block (결정적)

#### 2.3 ✅ 전략 1: 우회 경로 A* (request_handler.py:650~674)
- `excluded_transit = {block_robot.current_node}` (+ IN_PLACE 선반 노드)
- `astar_with_time(turn_penalty=0.3, start_heading=yield.heading)`
- 발견 + 첫 명령 send 성공 → 종료 (line 670)
- 첫 명령 즉시 재차단 시 → Strategy 2로 fall-through (line 673)

#### 2.4 ✅ 전략 2: 옆 노드 비켜주기 (request_handler.py:676~708)
- `_find_yield_node()` — contested 아닌 + 선반 아닌 + 미점유/미예약 인접 노드
- yield_node 도착 후 heading은 `_node_direction(current, yield) → degree`로 계산
- 두 번째 A* 호출로 yield_node→goal 경로 산출
- 전체 경로: `[current, yield] + path_from_yield[1:]`
- `_path_to_commands(full_path, yield.heading)` — turn + forward 시퀀스

#### 2.5 ✅ A* with time 시간 기반 충돌 회피 (path_planner.py:77~171)
- state = (node, time, dir) 3차원 탐색
- `(nxt_node, nt) in reserved_nodes` 노드 충돌 체크
- `(nxt_node, cur_node, t) in reserved_edges` 스왑 충돌 체크
- 자기 자리 대기 옵션: `neighbors.append((cur_node, 1.0))` — 시간차 만들기 가능
- `turn_penalty=0.3` (회전 최소화)
- start_heading degree → dir 매핑 정상 (`{0:0, 90:1, 180:2, 270:3}`)

#### 2.6 ✅ `_path_to_commands()` (request_handler.py:1271~1318)
- node_path 순회하며 turn + forward 생성
- diff 0/90/180/270에 따라 turn_right/turn_180/turn_left 매핑
- heading 누적 갱신 (i 단계마다)

#### 2.7 ⚠️ **의심 1**: 3-cycle 교착 미지원
- `_get_blocker_of`는 직접 차단자 1개만 반환
- A→B→C→A 시나리오 감지 불가
- **현재 시스템**: 2 AGV → 3-cycle 불가능 → OK
- **확장 시**: 3+ AGV로 늘리면 재설계 필요 (cycle detection 알고리즘)

#### 2.8 ⚠️ **의심 2**: 전략 2 실패 시 영구 stuck
- `_find_yield_node` None 반환 시 (line 678~680) 로그만 남기고 return
- yield_robot은 영구 blocked → 자동 해제 메커니즘 없음
- block_robot의 별도 액션 발생 시 `_retry_blocked_robots`가 다시 발동되지만 결과 동일
- **권장**: 전략 3 (block 로봇이 양보 — 우선순위 역전) 또는 사용자 알림 + 수동 개입

#### 2.9 ⚠️ **의심 3**: yield 중 carrying_shelf 안전성 부분 검증
- yield_robot이 carrying이면 IN_PLACE 선반 노드는 `shelf_excluded`로 제외됨 (line 647~648)
- **BUT**: yield_node가 RETURNING 중인 또 다른 로봇의 목적지(home_node)와 겹치는 케이스 미체크
- **영향**: 시뮬에선 거의 발생 안 함, 실물에선 위치 desync 가능
- **권장**: `_find_yield_node`에 다른 로봇의 planned_path[-1] (=목적지) 제외 조건 추가 검토

#### 2.10 📝 **메모**: max_time=50 한도
- 8×6 맵에서 통상 충분 (대각 ~12, 회피 포함 ~30)
- 다중 회피 동시 시 시간 폭발 가능 → A* None 반환 → 조기 발견 가능 (line 1462 로그)

### 결론

**상태**: 2-cycle 교착에 대한 2단계 전략(우회 경로 → 옆 칸 비켜주기)이 잘 구현됨. 시간 기반 A*로 동적 회피도 지원.

**주요 리스크**:
1. 의심 2 (전략 2 실패 시 영구 stuck) — 좁은 통로 + 선반 노드 인접 시 발현 가능
2. 의심 1 (3-cycle 미지원) — 2 AGV 한정 → 현재는 OK
3. 의심 3 (yield 노드 안전성 부분 검증) — 실물에서 잠재 위험

**권장 개선**:
- 전략 2 실패 시 fallback (예: 우선순위 역전, 사용자 알림)
- `_find_yield_node`에 목적지 노드 제외 조건 추가

---

## 영역 3: 인터셉트 (Node U)

**관련 파일**: `server/request_handler.py`, `server/task_manager.py`, `server/shelf_manager.py`

**핵심 시나리오**:
- AGV가 선반 반납 중(`RETURNING_SHELF`)에 같은 선반에 대한 신규 주문 도착
- 반납 취소 → 새 작업대로 우회 (`DELIVERING_TO_WS`로 전환)
- 이전 WS 회랑 자동 해제 필요

### 체크 항목

#### 3.1 ✅ 인터셉트 트리거 (request_handler.py:270)
- `_try_assign_pending_tasks` 내, 사용 가능 로봇 없을 때 호출
- 새 task의 첫 선반을 RETURNING_SHELF 중인 로봇이 들고 있으면 트리거

#### 3.2 ✅ 인터셉트 조건 (request_handler.py:347~369)
- 첫 선반 존재 (`task.shelf_sequence[0]`)
- 그 선반을 carrying 중인 로봇 존재
- `status == RETURNING_SHELF` (line 355) — RETURN 중에만 인터셉트
- `current_subtask.subtask_type == RETURN_SHELF` (line 368) — 이중 검증

#### 3.3 ✅ 서브태스크 mutation (request_handler.py:373~374)
- `current_st.subtask_type = FORWARD_SHELF`
- `current_st.target_node = task.workstation_id`
- 후속 처리는 `_handle_putdown_ack` FORWARD_SHELF 분기(line 919~)에서 정상 처리됨

#### 3.4 ✅ 로봇 상태 전환 (line 376)
- RETURNING_SHELF → DELIVERING_TO_WS

#### 3.5 ✅ ~~**NG 1**~~ 수정 완료 (2026-04-29, FLOWCHART 수정 27): 인터셉트 inline corridor 해제 — `is_exiting=False` 리셋 누락
- 위치: request_handler.py line 380~399 (인라인 corridor 해제 코드)
- `staging_manager.release_corridor_without_trigger()` (line 162~192)와 비교 시:
  - **누락 1**: `corridor.is_exiting = False` 리셋
  - **누락 2**: `corridor.state = CorridorState.OCCUPIED` 명시 (queue 있을 때)
- **영향**:
  - 새로 점유한 AGV가 OCCUPIED + is_exiting=True 상태로 시작
  - `check_position_release`가 corridor_area 밖 이동 시 새 AGV를 잘못 해제할 수 있음
  - 시뮬에서 우연히 동작할 수도 있으나 회귀 위험 큼
- **수정 방법**: inline 19줄(380~399)을 다음과 같이 교체
  ```python
  for ws_node, corridor in self.staging_manager.corridors.items():
      if corridor.occupying_rid == carrying_robot.rid:
          released = self.staging_manager.release_corridor_without_trigger(
              ws_node, carrying_robot.rid
          )
          if released:
              released_robot = self.robot_manager.get_robot(released.rid)
              if released_robot:
                  self._plan_and_publish_move(
                      released_robot.rid, released_robot.current_node, ws_node
                  )
          break
  ```
  → 수정 24 패턴(중복 로직 위임)과 일치

#### 3.6 ✅ 새 경로 발행 (request_handler.py:403)
- `_plan_and_publish_move(carrying_robot.rid, current_node, target_ws)`
- 새 WS의 staging 진입은 `should_stage` 자동 체크 (line 1396)

#### 3.7 ⚠️ **의심 1**: 새 task의 절차 흡수 시점
- 인터셉트 시점엔 carrying_task만 mutate, 새 task는 PENDING 유지
- 새 task의 WAIT/PICKUP/RETURN 인서트는 carrying_robot이 새 WS 도착 후(`_handle_putdown_ack` FORWARD_SHELF 분기, line 928~947)에 발생
- → 이 흐름이 PENDING 상태의 새 task를 정상적으로 흡수하는지 시나리오 검증 필요
- 코드상 `handle_shelf_forwarded(shelf_id, dest_ws)` (line 935)가 dest_ws 매칭 task를 찾아 처리

#### 3.8 ⚠️ **의심 2**: 인터셉트 시 reservation 해제 (영역 1 의심 3와 동일)
- carrying_robot이 forward 진행 중이면 `_reserved_nodes`에 next_node 등록되어 있을 수 있음
- 인터셉트 후 `_plan_and_publish_move`가 새 path로 갈아끼움 — 기존 reservation은 마커 도착 시 자동 해제
- 즉시 충돌은 없지만, 새 path의 첫 forward와 commit 중인 forward가 시간 겹침 가능
- **권장**: 인터셉트 진입 시 명시적 reservation clear

#### 3.9 ✅ 인터셉트와 WAIT_PICKING의 분리
- WAIT_PICKING 시 같은 선반 신규 주문 → `_decide_shelf_action`에서 forward 결정 (task_manager.py:329)
- RETURNING_SHELF 시 같은 선반 신규 주문 → `_try_intercept_returning_shelf` 발동
- 두 경로 모두 결과적으로 FORWARD_SHELF로 수렴 → 일관성 OK

### 결론

**상태**: 인터셉트의 핵심 로직(상태 전환, 서브태스크 mutation, 경로 재발행)은 정확. 단, **corridor 해제 inline 코드의 누락**은 명확한 NG.

**주요 리스크**:
1. **NG 1** (corridor.is_exiting 미리셋) — 즉시 수정 권장 (수정 24 패턴으로 정리)
2. 의심 1 (새 task 흡수 검증) — Stage 2 시나리오 테스트로 확인
3. 의심 2 (reservation 잔존) — 영역 1 의심 3과 묶어서 처리

**권장 개선**:
- NG 1 즉시 수정 (5줄 정도, 간단)
- 인터셉트 시나리오를 pytest로 자동화 (Stage 2 우선순위)

---

## 영역 4: STG (스테이징 게이팅)

**관련 파일**: `server/staging_manager.py`, `server/request_handler.py`

**핵심 메커니즘**:
- `should_stage()`: 작업대 회랑 점유 시 staging 노드에 대기
- `handle_marker_trigger()`: 퇴출 로봇이 트리거 마커 통과 시 대기 AGV 해제
- `release_corridor_without_trigger()`: FORWARD_SHELF 시 즉시 해제 (수정 12)
- `_waiting_robots.add(rid)`: corridor 해제 시 resume 발행 트리거 (수정 23)

### 체크 항목

#### 4.1 ✅ `should_stage()` 진입 분기 (staging_manager.py:84~113)
- FREE → OCCUPIED 전환 + occupying_rid 등록 + None(즉시 진입)
- 동일 rid 재호출 → 이미 인증됨 (None)
- 다른 AGV 점유 중 → staging_node 반환
- 호출 시 `_check_timeout` 자동 실행 (line 96)

#### 4.2 ✅ 큐 관리 `add_staged_agv()` (line 115~124)
- deque.append → FIFO 보장
- StagedAGV 데이터클래스에 staging_node + target_ws + staged_at 저장

#### 4.3 ✅ `handle_marker_trigger()` (line 138~160)
- `_trigger_to_ws` 역매핑으로 trigger node ↔ WS 매칭
- `release_corridor_without_trigger`로 위임 (수정 24 패턴 ✓)

#### 4.4 ✅ `release_corridor_without_trigger()` (line 162~192)
- `occupying_rid != rid` 시 skip — 수정 22 Fix B 방어 체크 ✓
- 큐 있을 시 next AGV 승계 + `is_exiting=False` 리셋
- 큐 없을 시 FREE 전환 + `is_exiting=False` 리셋
- `corridor.state = CorridorState.OCCUPIED` 명시 (queue 있을 때)

#### 4.5 ✅ `mark_exiting()` (line 194~204)
- state=OCCUPIED + occupying_rid 갱신 + occupied_at(타임아웃 재시작) + is_exiting=True

#### 4.6 ✅ `check_position_release()` (line 206~225)
- 모든 corridor 순회: occupying_rid 매칭 + OCCUPIED + is_exiting=True
- corridor_area (ws_node + gateway_node) 밖 이동 시 해제
- → trigger 노드 없는 경로(인터셉트 후)에도 동작

#### 4.7 ✅ `_check_timeout()` (line 227~245)
- STAGING_TIMEOUT=30초
- 큐 있으면 next AGV로 승계 + `pending_timeout_releases`에 등록
- request_handler가 다음 `_plan_and_publish_move` 진입 시 드레인 (request_handler.py:1413~1421)
- 큐 없으면 FREE

#### 4.8 ✅ marker_report 흐름 통합 (request_handler.py:441~482)
- `check_position_release` 호출 → 해제 시 `_plan_and_publish_move`로 다음 AGV 진입
- staged robot 미도착 케이스 (`current_node != staging_node`) → `_staged_to_ws` 등록 후 도착 대기 (수정 23 cmd-based 등가물 ✓)
- `is_staged_agv` 시 명령 보류 (line 463~465) — `staging_wait` 액션 반환

#### 4.9 ✅ `handle_marker_trigger` 호출 조건 (request_handler.py:470)
- `status == RETURNING_SHELF`일 때만 trigger 처리 (입장 경로의 trigger 통과 무시)
- FORWARD_SHELF는 새 corridor의 should_stage가 처리 → trigger 무시 OK

#### 4.10 ⚠️ **의심 1**: `should_stage` 진입 시 `is_exiting` 리셋 누락 (방어 부족)
- corridor.state=FREE → OCCUPIED 전환 시 `is_exiting`은 이전 값 유지
- 정상 흐름에선 release 시 False로 리셋됨 → OK
- BUT: 영역 3 NG 1(인터셉트 inline 해제)이 미수정이면 is_exiting=True 잔존 가능
- → 다음 점유자가 OCCUPIED + is_exiting=True 상태로 시작 → check_position_release가 오작동
- **권장**: `should_stage`에서 `corridor.is_exiting = False` 명시 리셋 (방어 코딩)

#### 4.11 ⚠️ **의심 2**: `_check_timeout` 승계 시 `is_exiting` 리셋 누락
- line 237~239: queue에서 다음 AGV로 승계만 처리, state/is_exiting 미터치
- mark_exiting된 후 timeout 발생 → is_exiting=True 잔존 → 새 점유자 오염
- **권장**: timeout 시 `release_corridor_without_trigger`로 위임 (수정 24 일관성)

#### 4.12 📝 **메모**: `pending_timeout_releases` 드레인 지연
- 다음 `_plan_and_publish_move` 진입 시 드레인 → 약간 지연 가능 (수십~수백 ms)
- 시뮬에선 문제 없음, 실물에서도 무시 가능

### 결론

**상태**: 핵심 흐름(should_stage → add_staged → handle_marker_trigger → release → resume)은 잘 구성됨. 위치 기반 해제도 cmd-based 환경에 맞게 정상 동작.

**주요 리스크**:
1. 의심 1 + 2 (is_exiting 리셋 누락) — 영역 3 NG 1과 연결됨. 단독으론 문제 없으나 방어 코딩 필요.
2. 인터셉트 inline 해제 (영역 3 NG 1)와 결합 시 corridor 상태 오염 가능

**권장 개선**:
- `should_stage`와 `_check_timeout`에 `is_exiting=False` 명시 리셋 추가
- `_check_timeout`을 `release_corridor_without_trigger`로 위임 (수정 24 패턴 일관성)

---

## 종합 요약

### 전체 진단

| 영역 | OK | 의심 | NG |
|------|-----|------|-----|
| 1. 충돌회피 | 6 | 3 | 0 |
| 2. 교착회피 | 6 | 3 | 0 |
| 3. 인터셉트 | 7 | 2 | 0 |
| 4. STG | 9 | 2 | 0 |
| **합계** | 28 | 10 | 0 |

**총평**: 핵심 알고리즘 로직은 27개 수정이 누적되어 잘 정돈됨. NG 1건은 2026-04-29 수정 27로 해결 완료.

### 발견된 NG / 의심 항목 (우선순위 순)

#### ✅ Priority 1: 수정 완료 (2026-04-29)

**[DONE] 영역 3 — 인터셉트 inline corridor 해제 → 위임 호출로 교체 (FLOWCHART 수정 27)**
- 19줄 inline → `staging_manager.release_corridor_without_trigger()` 단일 호출
- `is_exiting=False` 리셋 + `state=OCCUPIED` 일관 처리 보장
- request_handler.py `_try_intercept_returning_shelf` 내부 정리됨
- pytest 회귀 테스트(`test_intercept_corridor_release`)로 추후 검증 예정

#### 🟠 Priority 2: 실물 환경 전 보강 (의심)

**[의심] 영역 1 #1.8 — 마커 미감지 시 예약 영구 lock**
- forward 발행 → `_reserved_nodes` 등록 → 마커 도착 못 함 → 영구 lock
- 시뮬에선 문제 없으나 실물 카메라 노이즈 시 위험
- 수정: cmd 발행 시각 기록 → N초 timeout → 강제 해제 + 알람
- 예상 작업 시간: 30분~1시간

**[의심] 영역 1 #1.9 + 영역 3 #3.8 — 재계획 시 reservation 미해제**
- `_plan_and_publish_move` 재호출 시 기존 reservation 미클리어
- forward 진행 중이면 next_node 도달까지 자동 해제됨 → 즉시 실패는 없으나 desync 위험
- 수정: 재계획 진입 시 `_clear_robot_reservation(rid)` 호출 추가

**[의심] 영역 4 #4.10 + #4.11 — `is_exiting` 리셋 누락 (방어)**
- `should_stage` 진입 시, `_check_timeout` 승계 시 명시 리셋 필요
- NG 1 수정 후엔 정상 흐름이지만 방어 코딩 권장
- 수정: 각 함수에 1줄씩 추가

#### 🟡 Priority 3: 향후 검토 (확장성/엣지케이스)

**[의심] 영역 2 #2.7 — 3-cycle 교착 미지원**
- 현재 2 AGV → 영향 없음. 3+ AGV 확장 시 재설계.

**[의심] 영역 2 #2.8 — 전략 2 실패 시 영구 stuck**
- `_find_yield_node` None 반환 시 처리 없음. 좁은 통로 + 선반 인접 시 발현 가능.

**[의심] 영역 2 #2.9 — yield 시 다른 로봇 목적지 미체크**
- `_find_yield_node`에 다른 로봇의 planned_path[-1] 제외 검토.

**[의심] 영역 1 #1.7 — None forward → 무한 retry**
- 명령 큐 생성 버그 시 발현. 빠른 발견 가능하지만 fail-fast 권장.

### Stage 2 (pytest 단위 시나리오) 우선순위

검증 인프라가 전혀 없는 상태(테스트 파일 0개, pytest 미설치)에서 시작.

**1순위 — 인프라 구축 (반나절)**
- `tests/` 디렉토리 + `pytest.ini` + `conftest.py`
- `MockMqttPublisher` (publish 호출 기록)
- `make_request_handler()` 픽스처 (config + 모든 매니저 초기화)
- `step_marker(rid, node)`, `step_cmd_ack(rid, cmd)` 헬퍼

**2순위 — 회귀 방지 테스트 (1일)**
1. `test_intercept_corridor_release` — Priority 1 NG 1 수정 검증 (`is_exiting=False` 보장)
2. `test_collision_same_node` — A,B가 같은 노드 노릴 때 한쪽만 forward
3. `test_deadlock_head_on` — 정면 교착 → 전략 1(우회) 또는 전략 2(yield)
4. `test_stg_gating_basic` — 회랑 점유 시 staging 노드 대기
5. `test_stg_release_via_trigger` — 트리거 마커 통과 시 대기 AGV 해제
6. `test_stg_release_position_based` — 위치 기반 해제 (인터셉트 등 trigger 미통과 케이스)

**3순위 — 엣지케이스 테스트 (반나절)**
7. `test_intercept_returning_shelf` — Node U 정상 흐름
8. `test_forward_shelf_full_cycle` — pickup → forward → wait → re-pickup → return
9. `test_replan_for_placed_shelf` — 선반 배치 후 다른 로봇 우회

### Stage 3 (Isaac Sim 통합 런타임) 우선순위

**선결 조건**: Step 6 런타임 검증 완료 (현재 미완)

**1순위 — 로깅 강화 (1~2시간)**
- `print()` → `logging` 전환 (충돌/교착/인터셉트/STG 영역만)
- 로그 포맷: `[timestamp] [level] [module] message`
- 파일 로그 추가 (`logs/server-YYYYMMDD-HHMMSS.log`)

**2순위 — 시나리오 자동화 (반나절)**
- `mqtt_test.py` 확장 — 시나리오 스크립트 모드
- 시나리오: `시작1 → 3초 대기 → 시작2 → 마커 일정 시퀀스 시뮬레이션`
- 또는 Isaac Sim에서 직접 실행 + 외부 자동화

**3순위 — 시나리오별 런타임 검증 (1~2일)**
DEMO_MODE=False 상태에서 5종 검증:
- 회랑 진입 충돌 → STG 정상 동작
- 두 AGV 정면 교착 → 우회 또는 yield
- RETURN 중 같은 선반 신규 주문 → 인터셉트 + 회랑 해제
- 포워딩 시나리오 (선반 두 WS 모두 필요)
- 마커 미감지 시뮬 (timeout 발현 확인)

각 시나리오 결과를 로그로 캡처 → 회귀 비교용으로 보관.

---

## 진행 로그

- 2026-04-29: 영역 1~4 정적 분석 완료, 종합 요약 작성. NG 1건 + 의심 10건 발견.
- 2026-04-29: Priority 1 NG 1 수정 완료 (FLOWCHART 수정 27).
- 2026-04-29: Stage 2 인프라 구축 + 회귀 테스트 20종 작성 완료 (20 passed).
  - 인프라: `tests/conftest.py` (MockMqttPublisher + handler 픽스처 + 헬퍼), `pytest.ini`
  - smoke 5종: 픽스처 sanity
  - intercept 2종: NG 1 회귀 방지 + RETURNING 가드
  - collision 4종: forward 차단/예약/retry/마커 도착 해제
  - stg 7종: should_stage 분기 / trigger release / 위치 기반 release / rid mismatch 가드
  - deadlock 2종: 정면 교착 yield + carrying 우선순위
- 다음 단계: Stage 3 (Isaac Sim 런타임) 또는 Priority 2 의심 항목 보강
